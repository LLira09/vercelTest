{
  "version": 2,
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [{ "source": "/contollers(.*)", "destination": "/contollers" }],
  "routes": [
    {
      "src": "/(.*)"
    }
  ]
}
